write a program to read 2 numbers and print 1 for addition, 2 for subtraction, 3 for multiplication and 4 for divion using the switch case

SOURCE CODE:

#include <stdio.h>
#include<math.h>
int main() {
  int a,b;
  scanf("%d%d",&a,&b);
  int ch;
  scanf("%d",&ch);
  switch(ch)
  {
      case 1:
        printf("Addition: %d",a+b);
        break;
      case 2:
      printf("Subtraction: %d",a-b);
      break;
      case 3:
      printf("Multiplication: %d",a*b);
      break;
      case 4:
      printf("Division: %d",a/b);
      break;
      default:
      printf("Enter valid number");
  }
   return 0;
}

OUTPUT:

15 84
3
Multiplication: 1260

