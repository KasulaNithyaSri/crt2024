SOURCE CODE:

#include <stdio.h>
#include<string.h>
int main() {
    char s1[50],s2[50],i;
    scanf("%s",s1);
    scanf("%s",s2);
    i=strcmp(s1,s2);
    if(i==0)
    printf("Strings are same");
    else
    printf("Strings are not same");
    return 0;
}
