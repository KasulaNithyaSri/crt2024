write a program to find the number of digits in a number 

SOURCE CODE:

#include <stdio.h>

int main() {
    int n,d,s,count=0;
    scanf("%d",&n);
    while(n>0){
        if(n!=0){
        d=n%10;
        n=n/10;
        count++;
        
        }
    }
     
    
     printf("%d",count);
     return 0;
}

OUTPUT:
153
3
